rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For the prototype, allow all reads and writes.
      // IN PRODUCTION, you should require authentication!
      allow read, write: if true;
    }
  }
}
